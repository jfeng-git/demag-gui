[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "demag-gui"
version = "0.1.0"
description = "A GUI application for demagnetization analysis"